# macOS
.DS_Store

# Environment / secrets (never commit API keys)
.env
.env.*
agent/.env

# Swift build artifacts
vm-helper/.build/
vm-helper/*.resolved

# Downloaded VM images (large binaries — run prepare_image.sh to generate)
images/

# VM runtime data
~/.vm-mcp/
